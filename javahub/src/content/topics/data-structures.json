{
  "id": "data-structures",
  "title": "Data Structures in Java",
  "group": "Data Structures",
  "estimatedMinutes": 90,
  "subtopics": [
    {
      "id": "complexity",
      "title": "Time & Space Complexity",
      "cards": [
        {
          "id": "ds-big-o-notation",
          "topicId": "data-structures",
          "subtopicId": "complexity",
          "title": "Big O Notation",
          "difficulty": "Beginner",
          "explanation": "Big O describes how an algorithm's time or space requirements grow relative to input size n, in the worst case, ignoring constants and lower-order terms. The most common classes from fastest to slowest: O(1) constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic, O(n²) quadratic, O(2ⁿ) exponential. When analysing code, nested loops typically multiply — one loop inside another is O(n²). Sequential loops add — two separate loops over n items are still O(n).",
          "codeExample": "// O(1) — same cost regardless of input size\nint first = list.get(0);\n\n// O(log n) — halves search space each step (binary search, TreeMap)\nint idx = Collections.binarySearch(sortedList, target);\n\n// O(n) — visits each element once\nfor (int x : list) { sum += x; }\n\n// O(n log n) — sort\nCollections.sort(list);\n\n// O(n²) — nested loops over same collection\nfor (int i = 0; i < n; i++)\n    for (int j = 0; j < n; j++)  // inner loop runs n times per outer step\n        process(arr[i], arr[j]);\n\n// O(n) not O(n²) — sequential loops, not nested\nfor (int x : list) { doA(x); }  // O(n)\nfor (int x : list) { doB(x); }  // O(n)\n// total: O(n) + O(n) = O(n)",
          "whyItMatters": "Big O is the shared vocabulary for discussing algorithm efficiency in interviews and code reviews. It tells you whether an algorithm will scale from 1 000 to 1 000 000 items.",
          "gotcha": "Big O is worst-case by convention, but average-case is often what matters (HashMap is O(1) average, O(n) worst). Always clarify which case you're discussing.",
          "complexityTable": [
            { "operation": "O(1)", "best": "—", "average": "constant", "worst": "array get, HashMap get", "space": "no extra" },
            { "operation": "O(log n)", "best": "—", "average": "logarithmic", "worst": "binary search, TreeMap", "space": "O(1)" },
            { "operation": "O(n)", "best": "—", "average": "linear", "worst": "linear scan, ArrayList copy", "space": "O(n)" },
            { "operation": "O(n log n)", "best": "—", "average": "linearithmic", "worst": "merge sort, Arrays.sort", "space": "O(n)" },
            { "operation": "O(n²)", "best": "—", "average": "quadratic", "worst": "nested loops, bubble sort", "space": "O(1)" }
          ]
        }
      ]
    },
    {
      "id": "hash-tables",
      "title": "Hash Tables",
      "cards": [
        {
          "id": "ds-hashmap-internals",
          "topicId": "data-structures",
          "subtopicId": "hash-tables",
          "title": "HashMap Internals",
          "difficulty": "Mid",
          "explanation": "A HashMap stores key-value pairs in an array of buckets. The bucket index is computed from the key's hashCode(). When two keys hash to the same bucket (collision), Java uses a linked list (and a red-black tree when the list exceeds 8 entries, since Java 8). The default load factor is 0.75 — when 75% of buckets are filled, the table doubles in size and rehashes all entries.",
          "codeExample": "Map<String, Integer> map = new HashMap<>();\nmap.put(\"alice\", 30);\nmap.put(\"bob\", 25);\n\n// O(1) average get\nint age = map.get(\"alice\");  // 30\n\n// Merge pattern (avoid get + put)\nmap.merge(\"alice\", 1, Integer::sum);  // increments alice's value by 1\n\n// computeIfAbsent — lazy initialization\nMap<String, List<String>> groups = new HashMap<>();\ngroups.computeIfAbsent(\"team-a\", k -> new ArrayList<>()).add(\"alice\");",
          "whyItMatters": "HashMap is the most used data structure in Java after arrays. Understanding internals lets you tune initial capacity, avoid rehashing on large loads, and debug subtle equals/hashCode bugs.",
          "gotcha": "If you override equals() you MUST override hashCode() consistently — objects that are equal must have the same hash code. Failing to do this breaks HashMap, HashSet, and any hash-based collection.",
          "jdkClass": "java.util.HashMap",
          "vsComparison": "Use HashMap for O(1) get/put with no ordering. Use LinkedHashMap to preserve insertion order. Use TreeMap for sorted key order (O(log n) operations).",
          "complexityTable": [
            { "operation": "Get", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Put", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Remove", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Contains key", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(1)" }
          ]
        },
        {
          "id": "ds-hashmap-equals-hashcode",
          "topicId": "data-structures",
          "subtopicId": "hash-tables",
          "title": "equals() and hashCode() Contract",
          "difficulty": "Mid",
          "explanation": "The Java equals/hashCode contract has two rules: (1) if a.equals(b) then a.hashCode() == b.hashCode() must be true, and (2) hashCode() must be consistent — calling it multiple times on the same object must return the same value as long as the object hasn't changed. Breaking rule 1 makes objects impossible to find in HashMap/HashSet after insertion.",
          "codeExample": "// Broken: equals without hashCode\npublic class Point {\n    int x, y;\n    @Override public boolean equals(Object o) {\n        if (!(o instanceof Point p)) return false;\n        return x == p.x && y == p.y;\n    }\n    // Missing hashCode! HashMap will never find inserted Points\n}\n\n// Correct\npublic class Point {\n    int x, y;\n    @Override public boolean equals(Object o) {\n        if (!(o instanceof Point p)) return false;\n        return x == p.x && y == p.y;\n    }\n    @Override public int hashCode() {\n        return Objects.hash(x, y);  // consistent with equals\n    }\n}",
          "whyItMatters": "This contract is one of the most common sources of subtle bugs in Java — objects disappear silently from sets and maps.",
          "gotcha": "Using a mutable field in hashCode() is dangerous — if you mutate the field after inserting the object into a HashSet, you'll never find it again because the hash changed.",
          "jdkClass": "java.util.Objects"
        },
        {
          "id": "ds-map-implementations",
          "topicId": "data-structures",
          "subtopicId": "hash-tables",
          "title": "Map Implementations Compared",
          "difficulty": "Mid",
          "explanation": "Java has four commonly used Map implementations with different ordering and thread-safety guarantees. HashMap: no ordering, O(1) average get/put, null keys allowed. LinkedHashMap: preserves insertion order (or access order if configured), slightly more memory. TreeMap: sorted by key natural order or a Comparator, O(log n) operations — useful for range queries. ConcurrentHashMap: thread-safe without locking the whole map (segment-level locking), no null keys or values.",
          "codeExample": "// HashMap — fastest, no ordering\nMap<String, Integer> hash = new HashMap<>();\nhash.put(\"banana\", 2); hash.put(\"apple\", 1);\n// iteration order not guaranteed\n\n// LinkedHashMap — insertion-ordered\nMap<String, Integer> linked = new LinkedHashMap<>();\nlinked.put(\"banana\", 2); linked.put(\"apple\", 1);\nlinked.forEach((k, v) -> System.out.println(k)); // banana, apple\n\n// TreeMap — sorted by key\nMap<String, Integer> tree = new TreeMap<>();\ntree.put(\"banana\", 2); tree.put(\"apple\", 1);\ntree.forEach((k, v) -> System.out.println(k)); // apple, banana\n// Range queries\ntree.headMap(\"b\").forEach(…);      // keys < \"b\"\ntree.subMap(\"a\", \"c\").forEach(…);  // keys in [a, c)\n\n// ConcurrentHashMap — thread-safe\nMap<String, Integer> concurrent = new ConcurrentHashMap<>();\nconcurrent.put(\"x\", 1);  // safe from multiple threads\n// null keys/values throw NullPointerException",
          "whyItMatters": "Picking the wrong Map implementation either loses ordering guarantees, introduces race conditions, or adds unnecessary overhead.",
          "gotcha": "ConcurrentHashMap forbids null keys and values — putting null throws NullPointerException immediately. This differs from HashMap, which allows one null key.",
          "jdkClass": "java.util.Map",
          "vsComparison": "HashMap: O(1), unordered. LinkedHashMap: O(1), insertion-ordered. TreeMap: O(log n), sorted. ConcurrentHashMap: O(1), thread-safe, no nulls.",
          "complexityTable": [
            { "operation": "HashMap get/put", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(n)" },
            { "operation": "LinkedHashMap get/put", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(n)" },
            { "operation": "TreeMap get/put", "best": "O(log n)", "average": "O(log n)", "worst": "O(log n)", "space": "O(n)" },
            { "operation": "ConcurrentHashMap get/put", "best": "O(1)", "average": "O(1)", "worst": "O(n)", "space": "O(n)" }
          ]
        }
      ]
    },
    {
      "id": "arrays-strings",
      "title": "Arrays & Strings",
      "cards": [
        {
          "id": "ds-string-vs-stringbuilder",
          "topicId": "data-structures",
          "subtopicId": "arrays-strings",
          "title": "String vs StringBuilder vs StringBuffer",
          "difficulty": "Beginner",
          "explanation": "String is immutable — every concatenation creates a new object. StringBuilder is mutable and not thread-safe, making it the right choice for building strings in a single thread. StringBuffer is identical to StringBuilder but all methods are synchronized, making it thread-safe but slower. In practice, prefer StringBuilder; StringBuffer is rarely needed since better concurrency tools exist.",
          "codeExample": "// Bad: O(n²) due to repeated String allocation\nString result = \"\";\nfor (int i = 0; i < 1000; i++) {\n    result += i;  // creates a new String each iteration!\n}\n\n// Good: O(n) with StringBuilder\nStringBuilder sb = new StringBuilder();\nfor (int i = 0; i < 1000; i++) {\n    sb.append(i);\n}\nString result = sb.toString();\n\n// Note: the compiler optimises simple + in a single expression\nString s = \"Hello\" + \" \" + \"World\"; // compiled to StringBuilder internally",
          "whyItMatters": "String concatenation in a loop is one of the most common performance bugs in Java. StringBuilder is the standard fix.",
          "gotcha": "The compiler auto-optimises String + in a single expression, but NOT across loop iterations. Only the loop case is actually slow.",
          "jdkClass": "java.lang.StringBuilder",
          "vsComparison": "String for immutable values, StringBuilder for single-threaded construction, StringBuffer only when truly needed across threads (rare — use other approaches instead).",
          "complexityTable": [
            { "operation": "Append", "best": "O(1)*", "average": "O(1)*", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Insert at index", "best": "O(n)", "average": "O(n)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "toString()", "best": "O(n)", "average": "O(n)", "worst": "O(n)", "space": "O(n)" }
          ]
        },
        {
          "id": "ds-array-vs-arraylist",
          "topicId": "data-structures",
          "subtopicId": "arrays-strings",
          "title": "Array vs ArrayList",
          "difficulty": "Beginner",
          "explanation": "A Java array is a fixed-size, contiguous block of memory that can hold primitives or objects. ArrayList is a resizable array backed by an Object[] that doubles in capacity when full (amortised O(1) add). Use a raw array when size is known and fixed, or when you need primitive performance (no boxing). Use ArrayList when you need dynamic resizing, the Collections API, or interoperability with streams.",
          "codeExample": "// Array — fixed size, primitives allowed\nint[] scores = new int[5];  // no boxing overhead\nscores[0] = 42;\n// scores[5] = 1;  // ArrayIndexOutOfBoundsException at runtime\n\n// ArrayList — dynamic, objects only\nList<Integer> list = new ArrayList<>();  // Integer, not int (boxed)\nlist.add(42);  // auto-boxes int → Integer\nlist.add(99);\nlist.remove(Integer.valueOf(42));  // removes by value, not index\n\n// Convert between them\nint[] arr = {1, 2, 3};\nList<Integer> fromArr = new ArrayList<>(Arrays.asList(1, 2, 3));\nint[] backToArr = list.stream().mapToInt(Integer::intValue).toArray();",
          "whyItMatters": "Knowing when to use each avoids unnecessary boxing overhead and unexpected resizing costs in hot paths.",
          "gotcha": "Arrays.asList() returns a fixed-size list backed by the array — you can set elements but not add or remove. Wrap it in new ArrayList<>() if you need a truly mutable list.",
          "jdkClass": "java.util.ArrayList",
          "vsComparison": "Array: fixed size, primitives, fastest random access. ArrayList: dynamic size, objects only, slightly more memory due to boxing and Object[] overhead.",
          "complexityTable": [
            { "operation": "Random access", "best": "O(1)", "average": "O(1)", "worst": "O(1)", "space": "O(1)" },
            { "operation": "Add at end", "best": "O(1)*", "average": "O(1)*", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Insert at index", "best": "O(n)", "average": "O(n)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Remove at index", "best": "O(n)", "average": "O(n)", "worst": "O(n)", "space": "O(1)" }
          ]
        }
      ]
    },
    {
      "id": "linked-lists",
      "title": "Linked Lists & Queues",
      "cards": [
        {
          "id": "ds-linkedlist-vs-arraylist",
          "topicId": "data-structures",
          "subtopicId": "linked-lists",
          "title": "LinkedList vs ArrayList",
          "difficulty": "Mid",
          "explanation": "LinkedList is a doubly-linked list where each node holds a value and references to the previous and next nodes. It has O(1) insert/remove at head or tail but O(n) random access. ArrayList has O(1) random access but O(n) insert/remove in the middle. In practice, ArrayList outperforms LinkedList for almost all workloads due to CPU cache locality — sequential traversal of an array is much faster than following heap pointers.",
          "codeExample": "// LinkedList as a Deque (double-ended queue) — its best use case\nDeque<String> deque = new LinkedList<>();\ndeque.addFirst(\"a\");   // O(1)\ndeque.addLast(\"b\");    // O(1)\ndeque.addFirst(\"z\");   // O(1)\nSystem.out.println(deque);  // [z, a, b]\ndeque.pollFirst();          // removes and returns \"z\"\n\n// For queue/stack use cases, prefer ArrayDeque\nDeque<String> fast = new ArrayDeque<>(); // better cache performance than LinkedList\nfast.push(\"x\");\nfast.pop();\n\n// LinkedList get(index) is O(n) — avoid in loops\nLinkedList<Integer> list = new LinkedList<>(List.of(1,2,3,4,5));\nint mid = list.get(2);  // traverses from head — slow for large lists",
          "whyItMatters": "Choosing LinkedList expecting faster inserts often makes things slower due to GC pressure and cache misses. Benchmark before assuming.",
          "gotcha": "Java's LinkedList implements both List and Deque. When you need a queue or stack, use ArrayDeque instead — it's faster and uses less memory.",
          "jdkClass": "java.util.LinkedList",
          "vsComparison": "ArrayList: fast random access, cache-friendly, slow mid-list inserts. LinkedList: fast head/tail ops, poor cache locality, high memory overhead (two pointers per node).",
          "complexityTable": [
            { "operation": "Get by index", "best": "O(1)", "average": "O(n)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Add at head/tail", "best": "O(1)", "average": "O(1)", "worst": "O(1)", "space": "O(1)" },
            { "operation": "Add at index", "best": "O(1)", "average": "O(n)", "worst": "O(n)", "space": "O(1)" },
            { "operation": "Remove head/tail", "best": "O(1)", "average": "O(1)", "worst": "O(1)", "space": "O(1)" }
          ]
        }
      ]
    }
  ]
}
